{% extends 'base.html.twig' %}

{% block title %}Éditer Post{% endblock %}

{% block body %}
    <h1>Éditer Post</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Mettre à jour</button>
    {{ form_end(form) }}

    <a href="{{ path('app_post_index') }}">Retour à la liste</a>

    {{ include('post/_delete_form.html.twig') }}
{% endblock %}